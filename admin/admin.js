(() => {
  const app = document.getElementById('app');
  if (app) {
    app.insertAdjacentHTML(
      'beforeend',
      '<div class="notice">Admin controls coming in Phase 4.</div>',
    );
  }
})();
